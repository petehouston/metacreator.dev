# Live reload for local development.
root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/compute ./cmd/compute"
  bin = "./tmp/compute"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 300
  stop_on_error = true

[log]
  time = true
